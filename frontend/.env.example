# Runtime configuration of the Frontend Component. Copy to .env.local (never committed) and fill in values.
# NEXT_PUBLIC_* values are inlined into the browser bundle at build time, so rebuild after changing them.

# Base URL of the Backend API. Default: http://127.0.0.1:8000
NEXT_PUBLIC_API_BASE_URL=
