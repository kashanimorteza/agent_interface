# Alembic configuration of the my_database package. The Instance and its file
# are resolved by migrations/env.py through the package's settings boundary
# (DATABASE__* variables), never from this file.
[alembic]
script_location = %(here)s/migrations
prepend_sys_path = .
file_template = %%(rev)s_%%(slug)s
timezone = UTC
