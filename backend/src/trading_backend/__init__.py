"""Backend Component of Trading Assistant.

A FastAPI application whose API layer exposes every shared Model's standard operations, whose Logic
layer implements Model Logic, and whose Data Access layer is the only route to the Database Interface.
The application object lives in ``trading_backend.main``.
"""

__version__ = "0.1.0"
