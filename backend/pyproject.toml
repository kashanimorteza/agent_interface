[project]
name = "backend"
version = "0.1.0"
description = "Trading Assistant backend — the API for every project model"
requires-python = ">=3.12"
dependencies = [
    "alembic>=1.19.1",
    "fastapi[standard]>=0.141.1",
    "sqlalchemy>=2.0.52",
]
