# <!--------------------------------------------------------------------------------- Meta -->
meta:
  path: ".interface/developer/platform/preferences.yaml"
  path_note: "The Preferences layer of the Platform Component — supported technical choices and defaults used when a Platform choice is unstated."
  purpose: "Technical choices for how and where the complete Target is composed and brought online."
  standard: "Agent Project Interface"
  version: "1.0"
  updated: "2026-09-09"


# <!--------------------------------------------------------------------------------- Policy -->
policy:
  agent_may_edit: false
  rule: "This file is human-owned. Operations read it without writing resolved Platform choices back into it."
  changed_by: "The human, directly. A missing critical technical choice is raised through State."
  precedence: "An explicit Target value and every applicable Platform Principle take precedence over this file."
  consumption: "Operations use these Preferences only when the Target leaves a Platform choice unstated."
  selection: "When no preference is defined, choose the smallest compatible option under the shared decision policy."
  version_resolution: "Resolve an unpinned version to a concrete compatible stable version and preserve an explicit concrete version."
  matching: "Match a Platform choice to the applicable entry when one exists."


# <!--------------------------------------------------------------------------------- Read_Order -->
read_order:
  - "selected"
  - "options"
  - "settings"


# <!--------------------------------------------------------------------------------- Content_Map -->
content_map:
  selected: "The default Platform choices, currently empty."
  options: "Supported Platform alternatives, currently empty."
  settings: "Platform configuration and composition defaults, currently empty."


# <!--------------------------------------------------------------------------------- Content -->
content:
  selected: {}
  options: {}
  settings: {}
