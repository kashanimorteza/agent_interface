# <!--------------------------------------------------------------------------------- Meta -->
meta:
  path: ".interface/developer/state/preferences.yaml"
  path_note: "The Preferences layer of the State Component. It currently defines no technical choices for operational progress, runtime state, or history."
  purpose: "Where technical choices owned by State would be recorded. State currently makes none."
  standard: "Agent Project Interface"
  version: "1.1"
  updated: "2026-09-10"


# <!--------------------------------------------------------------------------------- Policy -->
policy:
  agent_may_edit: false
  rule: "This file is human-owned and is read only when resolving State preferences."
  changed_by: "The human, directly."
  precedence: "An explicit project value and every applicable Principle take precedence over this file."
  consumption: "The empty content provides no State defaults; consumers continue with the other applicable authorities."
  selection: "There is nothing to select. A technical need that appears to belong to State is resolved by the Component that owns the technology, or raised through State when no Component owns it."
  version_resolution: "No entry here carries a version, because no technology is recorded."
  matching: "There is nothing to match. An empty section is an absence of defaults, not a wildcard."


# <!--------------------------------------------------------------------------------- Read_Order -->
read_order:
  - "selected"
  - "options"
  - "settings"


# <!--------------------------------------------------------------------------------- Content_Map -->
content_map:
  selected: "Empty. The State Component chooses among no catalogued alternatives and owns no code path."
  options: "Empty. The State Component catalogues no alternatives."
  settings: "Empty. The State Component configures nothing."


# <!--------------------------------------------------------------------------------- Content -->
content:

  # <!-------------------------------------------- selected -->
  selected: {}

  # <!-------------------------------------------- options -->
  options: {}

  # <!-------------------------------------------- settings -->
  settings: {}
