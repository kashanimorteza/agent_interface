# <!--------------------------------------------------------------------------------- Meta -->
meta:
  path: ".interface/developer/plan/preferences.yaml"
  path_note: "The Preferences layer of the Plan Component. It currently defines no Component-specific technical choices or defaults."
  purpose: "Where the Plan Component's technical choices would be recorded. The Plan Component currently makes none."
  standard: "Agent Project Interface"
  version: "1.0"
  updated: "2026-09-07"


# <!--------------------------------------------------------------------------------- Policy -->
policy:
  agent_may_edit: false
  rule: "This file is human-owned and is read only when resolving Plan preferences."
  changed_by: "The human, directly."
  precedence: "An explicit project value and every applicable Principle take precedence over this file."
  consumption: "The empty content provides no Plan defaults; consumers continue with the other applicable authorities."
  selection: "There is nothing to select. A technical need that appears to belong to Task is resolved by the Component that owns the technology, or raised through State when no Component owns it."
  version_resolution: "No entry here carries a version, because no technology is recorded."
  matching: "There is nothing to match. An empty section is an absence of defaults, not a wildcard."


# <!--------------------------------------------------------------------------------- Read_Order -->
read_order:
  - "selected"
  - "options"
  - "settings"


# <!--------------------------------------------------------------------------------- Content_Map -->
content_map:
  selected: "Empty. The Plan Component chooses among no catalogued alternatives and owns no code path."
  options: "Empty. The Plan Component catalogues no alternatives."
  settings: "Empty. The Plan Component configures nothing."


# <!--------------------------------------------------------------------------------- Content -->
content:

  # <!-------------------------------------------- selected -->
  selected: {}

  # <!-------------------------------------------- options -->
  options: {}

  # <!-------------------------------------------- settings -->
  settings: {}
