meta:
  path: .interface/agent/command/preferences.yaml
  path_note: The Preferences layer of the Agent Command Component.
  purpose: Current Agent Command selections and declarations.
  standard: Agent Project Interface
  version: '3.0'
  updated: '2026-09-21'

policy:
  agent_may_edit: false
  rule: This file is human-owned and currently contains no selections or declarations.
  changed_by: The human, directly.
  precedence: An explicit project decision and every applicable Principle take precedence over this file.

resolution:
  consumption: No Agent Command preference is currently declared.
  selection: No selection is currently declared.
  version_resolution: No version resolution is currently declared.
  matching: No matching rule is currently declared.

read_order:
  - selected
  - options
  - settings

content_map:
  selected: No selections are currently declared.
  options: No options are currently declared.
  settings: No settings or declarations are currently declared.

content:
  selected: {}
  options: {}
  settings: {}
