# <!--------------------------------------------------------------------------------- Meta -->
meta:
  path: ".interface/implementation/development/platform/platform.yaml"
  path_note: "The Preferences layer of the Platform Component — named Launch Items with per-Component runtime requirements."
  purpose: "Technical defaults for selecting and applying a complete launch definition to the Target."
  standard: "Agent Project Interface"
  version: "5.0"
  updated: "2026-09-14"


# <!--------------------------------------------------------------------------------- Policy -->
policy:
  agent_may_edit: false
  rule: "This file is human-owned. Operations read it without writing resolved Platform choices back into it."
  changed_by: "The human, directly. A missing critical Platform choice is raised through State."
  precedence: "An explicit Target value and every applicable Platform Principle take precedence over this file."


# <!--------------------------------------------------------------------------------- Resolution -->
resolution:
  consumption: "Resolve selected.launch to its exact entry under options.launches, then apply each selected Component's runtime requirements as one coherent launch definition. Do not infer undeclared actions and do not write resolved values back into this file."
  selection: "Use the selected Launch Item when the Target leaves the launch method unstated. For an explicit unlisted or incompatible need, select the smallest compatible definition and record the consequential choice; ask only when no safe compatible definition can be established."
  version_resolution: "Preserve every explicit concrete version. Resolve an unpinned version to a concrete compatible stable version when a versioned package or tool is added to a Launch Item."
  matching: "Match selected.launch to its exact key under options.launches and each Component's runtime requirements by its exact component key."




# <!--------------------------------------------------------------------------------- Read_Order -->
read_order:
  - "selected"
  - "options"
  - "settings"


# <!--------------------------------------------------------------------------------- Content_Map -->
content_map:
  selected: "The default named Launch Item."
  options: "The supported named Launch Items, each containing runtime requirements for their applicable Components."
  settings: "Empty. The complete current Platform configuration is owned by each Launch Item."


# <!--------------------------------------------------------------------------------- Content -->
content:

  # <!-------------------------------------------- selected -->
  selected:
    launch: "VPS"

  # <!-------------------------------------------- options -->
  options:
    launches:
      VPS:
        presentation:
          operating_system: "Linux"
        api:
          operating_system: "Linux"
          web_server: "Nginx"
          runtime_binding:
            host: "0.0.0.0"
            port: 8000
            transport: "HTTP"
            cors:
              allowed_origins: []
            shutdown:
              graceful_timeout_seconds: 30
            infrastructure_rate_limiting:
              enabled: false
        database:
          operating_system: "Linux"

  # <!-------------------------------------------- settings -->
  settings: {}
