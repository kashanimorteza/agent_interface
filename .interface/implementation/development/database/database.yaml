# <!--------------------------------------------------------------------------------- Meta -->
meta:
  path: ".interface/implementation/development/database/database.yaml"
  path_note: "Preferences for the Database Component."
  purpose: "Implementation layout and conventions for the Database Component."
  standard: "Agent Project Interface"
  version: "11.0"
  updated: "2026-09-24"


# <!--------------------------------------------------------------------------------- Policy -->
policy:
  agent_may_edit: false
  rule: "This file is human-owned."
  changed_by: "The human, directly."


# <!--------------------------------------------------------------------------------- Resolution -->
resolution:
  consumption: "Read database.yaml for the current Engines, Instances, Settings, and Operations. This file defines only layout and conventions."
  matching: "Each declared Engine uses one implementation file beneath settings.layout.engine_directory."


# <!--------------------------------------------------------------------------------- Content -->
content:

  # <!-------------------------------------------- settings -->
  settings:
    configuration:
      file: "database.yaml"
      structure: ".interface/foundation/schema/database.yaml"

    layout:
      public_interface: "One public entry point for the Operations declared in database.yaml."
      mapping: "One internal Mapping layer between Public Interface and Engine implementations; it routes requests and standardizes results when needed."
      engine_directory: "engine"
      engine_file: "One implementation file per declared Engine, named by that Engine key in the selected language standard."
