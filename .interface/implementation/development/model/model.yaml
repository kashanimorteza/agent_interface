# <!--------------------------------------------------------------------------------- Meta -->
meta:
  path: ".interface/implementation/development/model/model.yaml"
  path_note: "The Preferences layer of the Model Component."
  purpose: "Configurable component, layout, data validation, and technical implementation choices for Model."
  standard: "Agent Project Interface"
  version: "13.0"
  updated: "2026-09-25"

# <!--------------------------------------------------------------------------------- Policy -->
policy:
  agent_may_edit: false
  rule: "This file is human-owned. Operations read it without writing resolved Model choices back into it."
  changed_by: "The human, directly. A missing critical Model choice is raised through State."
  precedence: "An explicit Model definition and every applicable Principle take precedence over this file."

# <!--------------------------------------------------------------------------------- Resolution -->
resolution:
  consumption: "Read settings for Model identity; read layout and data_validation for Model's defaults and data checks; then read Model's development section for capabilities, naming, and technical implementation choices. Use Development Defaults only when Model leaves a shared choice unstated. Preserve every explicit Target property, including false and null, and never write resolved values back into this file."
  selection: "When the Target leaves a Model-owned choice unstated and this file supplies no default, use compatible professional judgment under Model Principles and record a consequential implementation choice with the implementation."
  version_resolution: "Model's development section resolves Model-owned versioned technical items. Development resolves only its shared Defaults."
  matching: "An explicit Target property takes priority. A Preference default applies only to a missing property of an existing Field when the Target leaves that property unstated; otherwise no default applies. A Preference never creates, removes, or renames Target meaning."

# <!--------------------------------------------------------------------------------- Read_Order -->
read_order:
  - "settings: identify the Model Component and its technical selection."
  - "layout: locate Model's Interface, Entity, Declaration, and Foundation layers and their configured names."
  - "data_validation: apply Entity responsibility and validation moments."
  - "development: apply Model capabilities, naming conventions, language, package, and implementation defaults."

# <!--------------------------------------------------------------------------------- Content_Map -->
content_map:
  settings: "The Model Component's type, language, platform, root directory, and package name."
  layout: "Entity directory, Interface, Declaration, and Foundation file names, class names, Foundation method names, and casing."
  data_validation: "The responsibility and moments for Entity data validation."
  development: "Component capabilities, Model naming conventions, and selected SQLModel implementation defaults."

# <!--------------------------------------------------------------------------------- Content -->
content:

  # <!-------------------------------------------- settings -->
  settings:
    component:
      type: "library"
      language: "Python"
      platform: "VPS"
      directory: "my_model"
      package: "my_model"

  # <!-------------------------------------------- layout -->
  layout:
    directories:
      entity: "entity"
    files:
      interface: "interface.py"
      declaration: "declaration.py"
      foundation: "foundation.py"
    classes:
      declaration: "Declaration"
      foundation: "Foundation"
    foundation:
      methods:
        to_json: "to_json"
        from_json: "from_json"
    casing: "Spell every name in the selected language's own casing standard, and name files and folders by the same standard, as development.languages states it."

  # <!-------------------------------------------- data_validation -->
  data_validation:
    responsibility: "Each Entity verifies its own data against its Declaration through Foundation."
    moments:
      - "direct construction"
      - "from_json"
      - "to_json"

  # <!-------------------------------------------- development -->
  development:
    capabilities:
      testing: false
      error_handling: false
      logging: false
      encryption: false
    naming:
      entity: "PascalCase"
      fields: "snake_case"
      modules: "snake_case"
    languages:
      python:
        modeling:
          package:
            name: "SQLModel"
            version: "latest-compatible"
            skill: "sqlmodel"
          guidance:
            project_rules:
              source_of_truth: "Generate each Model from its Domain Entity. Do not add Fields, constraints, defaults, or behavior that the Entity does not declare."
              declaration: "Every Model uses the configured public Declaration class to record its technology-independent Fields, Types, Field Rules, Primary Key, Uniqueness Constraints, Relationships, Index intentions, and Value Generation. Declaration provides no runtime behaviour."
              entity: "Generate every Entity as a public SQLModel table model. SQLModel realizes the declared Fields and metadata without redefining their domain meaning."
              foundation: "Every Entity uses the configured public Foundation class to verify its own data against Declaration during direct construction, from_json, and to_json. Foundation uses SQLModel for conversion and validation while retaining the public to_json and from_json contract."
              entity_unit: "Create one public SQLModel Entity class per Domain Entity and place it in its own module beneath the configured Entity directory."
              interface: "Keep Interface, Entity, Declaration, and Foundation public. Interface presents consumer-facing Entity classes and capabilities as a convenient entry point."
              flat_structure: "Do not nest, inherit, or copy Entities. Record every Target-declared cross-Entity connection as an explicit Relationship or Reference."
  
  # <!-------------------------------------------- documentation -->
  documentation:
    file: "README.md"
    location: "the Component root"
    format: "markdown"
    order:
      - overview
      - interface
      - setup
      - run
      - verify
      - troubleshooting
