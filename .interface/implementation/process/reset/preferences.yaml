# <!--------------------------------------------------------------------------------- Meta -->
meta:
  path: ".interface/implementation/process/reset/preferences.yaml"
  path_note: "The Preferences layer of the Reset Component."
  purpose: "Reset defaults used when the selected scope and owning authorities are silent."
  standard: "Agent Project Interface"
  version: "1.0"
  updated: "2026-09-20"

# <!--------------------------------------------------------------------------------- Policy -->
policy:
  agent_may_edit: false
  rule: "This file is human-owned. Reset reads it without writing resolved choices back into it."
  changed_by: "The human, directly. A missing critical Reset choice is raised through State."
  precedence: "An explicit Target value and every applicable Principle take precedence over this file."

# <!--------------------------------------------------------------------------------- Resolution -->
resolution:
  consumption: "Apply a Reset default only when the authorized scope and owning authorities are silent."
  selection: "Refuse an ambiguous or unsafe reset and raise the required human decision."
  version_resolution: "No entry currently carries a version."
  matching: "Match reset settings to the explicitly selected record or output scope."

# <!--------------------------------------------------------------------------------- Read_Order -->
read_order:
  - "selected"
  - "options"
  - "settings"

# <!--------------------------------------------------------------------------------- Content_Map -->
content_map:
  selected: "Empty. Reset selects no shared reset profile."
  options: "Empty. Reset catalogues no shared alternatives."
  settings: "Empty. Reset defaults remain to be declared when needed."

# <!--------------------------------------------------------------------------------- Content -->
content:
  selected: {}
  options: {}
  settings: {}
