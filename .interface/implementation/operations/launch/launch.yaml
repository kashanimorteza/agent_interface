# <!--------------------------------------------------------------------------------- Meta -->
meta:
  path: ".interface/implementation/operations/launch/launch.yaml"
  path_note: "The Preferences layer of the Launch Component."
  purpose: "Launch defaults used when Platform and Operations authorities are silent."
  standard: "Agent Project Interface"
  version: "1.0"
  updated: "2026-09-20"

# <!--------------------------------------------------------------------------------- Policy -->
policy:
  agent_may_edit: false
  rule: "This file is human-owned. Launch reads it without writing resolved choices back into it."
  changed_by: "The human, directly. A missing critical Launch choice is raised through State."
  precedence: "An explicit Target value and every applicable Principle take precedence over this file."

# <!--------------------------------------------------------------------------------- Resolution -->
resolution:
  consumption: "Apply a Launch default only when Platform and Operations authorities are silent."
  selection: "Use the safest compatible launch behavior and record a consequential choice when no preference applies."
  version_resolution: "No entry currently carries a version."
  matching: "Match launch settings to the selected Platform and launch requirements."

# <!--------------------------------------------------------------------------------- Read_Order -->
read_order:
  - "selected"
  - "options"
  - "settings"

# <!--------------------------------------------------------------------------------- Content_Map -->
content_map:
  selected: "Empty. Launch selects no shared activation profile."
  options: "Empty. Launch catalogues no shared alternatives."
  settings: "Empty. Launch defaults remain to be declared when needed."

# <!--------------------------------------------------------------------------------- Content -->
content:
  selected: {}
  options: {}
  settings: {}
