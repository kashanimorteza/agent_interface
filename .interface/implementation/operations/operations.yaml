# <!--------------------------------------------------------------------------------- Meta -->
meta:
  path: ".interface/implementation/operations/operations.yaml"
  path_note: "The Preferences layer of the Operations Subsystem — defaults shared by its Operation Components."
  purpose: "Shared Operations choices used when the Target and owning Operation Component are silent."
  standard: "Agent Project Interface"
  version: "1.0"
  updated: "2026-09-20"

# <!--------------------------------------------------------------------------------- Policy -->
policy:
  agent_may_edit: false
  rule: "This file is human-owned. Operations read it without writing resolved choices back into it."
  changed_by: "The human, directly. A missing critical Operations choice is raised through State."
  precedence: "An explicit Target value and every applicable Principle take precedence over this file."

# <!--------------------------------------------------------------------------------- Resolution -->
resolution:
  consumption: "Apply a shared Operations default only when neither the Target nor the owning Operation Component supplies the choice."
  selection: "There is currently no shared Operations alternative to select."
  version_resolution: "No entry currently carries a version."
  matching: "There is currently no shared Operations entry to match."

# <!--------------------------------------------------------------------------------- Read_Order -->
read_order:
  - "selected"
  - "options"
  - "settings"

# <!--------------------------------------------------------------------------------- Content_Map -->
content_map:
  selected: "Empty. Operations selects no shared technical profile."
  options: "Empty. Operations catalogues no shared alternatives."
  settings: "Empty. Each Operation Component currently owns its own settings."

# <!--------------------------------------------------------------------------------- Content -->
content:
  selected: {}
  options: {}
  settings: {}
