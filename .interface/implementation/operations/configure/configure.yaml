# <!--------------------------------------------------------------------------------- Meta -->
meta:
  path: ".interface/implementation/operations/configure/configure.yaml"
  path_note: "The Preferences layer of the Configure Component."
  purpose: "Configure has no independent choices; its four Config Records follow their Schemas."
  standard: "Agent Project Interface"
  version: "2.0"
  updated: "2026-09-20"

# <!--------------------------------------------------------------------------------- Policy -->
policy:
  agent_may_edit: false
  rule: "This file is human-owned. Configure reads it without writing resolved choices back into it."
  changed_by: "The human, directly. A missing structural requirement is resolved by the owning Config Schema."
  precedence: "The four Config Schemas and every applicable Principle take precedence over this file."

# <!--------------------------------------------------------------------------------- Resolution -->
resolution:
  consumption: "Configure uses the four Config Schemas directly; this file supplies no defaults."
  selection: "There is no Configure alternative to select."
  version_resolution: "No entry currently carries a version."
  matching: "There is no Configure entry to match."

# <!--------------------------------------------------------------------------------- Read_Order -->
read_order:
  - "selected"
  - "options"
  - "settings"

# <!--------------------------------------------------------------------------------- Content_Map -->
content_map:
  selected: "Empty. Configure selects no profile."
  options: "Empty. Configure supports no alternatives."
  settings: "Empty. Configure has no independent settings."

# <!--------------------------------------------------------------------------------- Content -->
content:
  selected: {}
  options: {}
  settings: {}
