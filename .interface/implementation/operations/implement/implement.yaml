# <!--------------------------------------------------------------------------------- Meta -->
meta:
  path: ".interface/implementation/operations/implement/implement.yaml"
  path_note: "The Preferences layer of the Implement Component. It currently defines no Component-specific technical choices or defaults."
  purpose: "Where the Implement Component's technical choices would be recorded. The Implement Component currently makes none."
  standard: "Agent Project Interface"
  version: "1.1"
  updated: "2026-09-21"

# <!--------------------------------------------------------------------------------- Policy -->
policy:
  agent_may_edit: false
  rule: "This file is human-owned. Implement reads it without writing resolved choices back into it."
  changed_by: "The human, directly. A missing critical Implement choice is raised through State."
  precedence: "An explicit Target value and every applicable Principle take precedence over this file."

# <!--------------------------------------------------------------------------------- Resolution -->
resolution:
  consumption: "Apply an Implement default only when the Operations authorities and selected phase are silent."
  selection: "Use the declared Operations sequence; raise an Open Question when safe coordination cannot be established."
  version_resolution: "No entry currently carries a version."
  matching: "Match coordination settings to the selected phase and its owning Operation Components."

# <!--------------------------------------------------------------------------------- Read_Order -->
read_order:
  - "selected"
  - "options"
  - "settings"

# <!--------------------------------------------------------------------------------- Content_Map -->
content_map:
  selected: "Empty. Implement selects no shared coordination profile."
  options: "Empty. Implement catalogues no shared alternatives."
  settings: "Implement-specific technical defaults remain empty."

# <!--------------------------------------------------------------------------------- Content -->
content:
  selected: {}
  options: {}
  settings: {}
