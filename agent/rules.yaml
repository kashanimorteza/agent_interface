meta:
  path: "rules.yaml"
  path_note: "The address of this file — layer 3, the global rules"
  purpose: "Rules governing agent behavior — security and quality, both in this file"
  completeness: "A rule that is not in this file is not a rule"
  reason: "Rules are the boundary of agent behavior — an agent never moves its own boundary"
  no_item_rules: "An item has no rules file of its own — every rule is here, and an item's specifics live in its definition.yaml"
  agent_may_edit: false
  updated: "2026-08-24"


environment:
  - "All Python work happens inside the item's virtual environment — never the system interpreter"
  - "Every dependency is installed into that environment and never globally"
  - "Every command in a task's verify runs from inside that environment — a result produced outside it does not count"
  - "The virtual environment is never committed"


code:
  - "Business logic stays in services — routes only accept input and return output"
  - "Direct database access from inside routes is forbidden — go through a service"
  - "Every API input is validated against a model — raw input never reaches a service"
  - "Error responses always have the same structure — code, message, details"


database:
  - "Every schema change comes with an Alembic migration — changing a table by hand is forbidden"
  - "A migration that has already run is never edited — create a new migration"
  - "Every migration must be reversible — write the downgrade"
  - "Do not write a query that filters on a column without an index"
  - "SQLite cannot alter a column or drop a constraint in place — any such migration uses Alembic's batch mode, or it will fail on upgrade"
  - "The database file path comes from configuration — never a hardcoded path, and never a path inside the repository"
  - "Foreign keys are off by default in SQLite — enable them on every connection, or ownership constraints are silently unenforced"


security:
  - "Passwords are stored hashed only — never in the clear"
  - "Session tokens are issued with an expiry"
  - "Every task-related route must check user ownership — a user only sees their own tasks"
  - "Error messages must not reveal whether an account exists"


test:
  - "Every API route has at least one success test and one error test"
  - "Tests run against a test database — not the development database"
  - "An ownership test is mandatory: user A must not be able to access user B's task"


contract:
  - "Changing the API surface without updating the contract is forbidden"
  - "A frozen contract does not change — create a new version"
