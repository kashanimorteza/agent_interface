"""Model library: the project's technology-independent Domain Entities.

Consumers use the ``interface`` layer only. The ``entity``, ``declaration`` and
``foundation`` layers are private implementation.
"""
