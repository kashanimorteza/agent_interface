"""Internal declarations of the initial records, one module per Model that has them.

Each module exposes RECORDS: the Model's initial records in the order the project definition
states them. Identifiers are left awaiting auto-increment generation, references keep the
identifiers the definition states, and values the definition leaves to secure generation await it.
"""
