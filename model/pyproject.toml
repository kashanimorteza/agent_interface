[project]
name = "my-model"
version = "0.1.0"
description = "Shared domain Model package: the single logical definition of every domain Model, its declarations, and its initial records."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pydantic>=2.13.5,<3",
]

[build-system]
requires = ["uv_build>=0.12.12,<0.13"]
build-backend = "uv_build"
